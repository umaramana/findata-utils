# Project Memory

## Prompting Guide
- **Full guide**: [prompting_guide.md](prompting_guide.md)
- **Bug report format**: "Page X (filename), row Y — expected [subtracted: 1000], got [balance: 1000]. Description reads: [exact text]"
- **Key rule**: Share exact text, not recollections. Filename, not "another file".
- **Efficiency log**: also in prompting_guide.md — target 75%, Phase 1 scored 65%

## Working Principles
- **Evaluate before planning**: Before diving into solution design, do a quick upfront check — what does the feature actually warrant? Ask: (1) What's the simplest version that solves the problem? (2) What assumptions are we making? (3) What are the data/environment constraints? See [patterns.md](patterns.md) for details.
- **Explain logic before coding**: Always explain the approach and get user approval before writing code. Don't run code first and explain after.
- **Collaborate, don't just execute**: Before committing to an approach, narrate it — "I'm choosing X over Y because Z — agree?" This is especially critical for architectural decisions. "Built it, here's the output" without prior alignment = low collaboration and often wasted turns.
- **Don't backtrack on approved decisions**: Once the user approves an approach (e.g., "Option C"), stick with it. Don't silently revert to a different approach in implementation.
- **Present options for non-trivial decisions**: Give 2-3 clear options with pros/cons. Let the user choose. Don't assume.
- **End-of-session memory update**: At the end of every significant build session, run efficiency analysis (see patterns.md) - first ask the user for the session cost (`/cost`) and use it for the score, never estimate spend - and save any new learnings to memory files. Never let valuable methodology or decisions go unsaved. Then sync `docs/` in the repo: copy updated memory files into `docs/`, commit, and push — so git history backs up the context.
- **Understand the data first**: Before solutioning, verify assumptions about the actual data (column counts, layouts, edge cases) rather than guessing from code logic alone.

## Project: Stock Transaction Processor
- **Location**: `stock_processor/` — Streamlit app converting broker 1099-B Excel files to Drake tax import format
- **Brokers supported**: Fidelity ✅, Merrill Lynch ✅, Morgan Stanley ✅, Betterment (CSV) ✅, Apex Clearing ✅, Robinhood ✅, JP Morgan ✅, Charles Schwab (parked — bug)
- **Key files**: `app.py` (Streamlit UI), `pdf_qc.py` (column QC), `drake_mapper.py`, `brokers/fidelity.py`, `brokers/schwab.py`, `brokers/robinhood.py`, `brokers/merrill.py`, `brokers/morgan_stanley.py`, `brokers/betterment.py`, `brokers/apex_clearing.py`, `brokers/jpmorgan.py`, `utils.py`, `broker_profiler.py`
- **Requirements doc**: `stock_processor/REQUIREMENTS.md`
- **Broker Profiler**: `python broker_profiler.py <file.xlsx|file.csv> [sheet_name]` — run on new broker files FIRST. Outputs column profiles, anchor detection, similarity ranking vs existing brokers. Use before building any new broker module.
- **pdf_qc.py**: Column QC — universal. Pass 1: RIGHT-shifts (date found right of expected → shift left). Pass 2: LEFT-shifts / empty-cell-collapse (Date Acquired and Gain/Loss shifted left when spacer/optional cols empty → move back right). See [stock_processor_qc.md](stock_processor_qc.md).
- **Three numeric anchors**: Proceeds | Cost | [Accrued?] | [Wash Sale?] | Gain/Loss. First 2 = always Proceeds/Cost. Last = always Gain/Loss (NEVER empty). Optionals between Cost and Gain/Loss. See [stock_processor_architecture.md](stock_processor_architecture.md).
- **Empty cell collapse**: PDF24 artifact — when optionals are absent, Gain/Loss slides left into optional positions. Fix: detect via Gain/Loss being empty at expected col, scan backward, move to correct position. Implemented as `_fix_empty_cell_collapse(df, cost_col_idx, gain_loss_col_idx)` in fidelity.py (standalone reusable fn).
- **Gain/Loss**: NEVER empty for real transaction rows. NOT in Drake output. Marks end of optional zone. Key rule: if Gain/Loss col is empty → collapse detected.
- **Parking lot**: Subtotal aggregation per stock; **Summary Page QC + Pass 3** (see below); Proceeds gap (~$4,640 / ~4 missing txns in Merrill test); Schwab test files in `testdata/` (parked); Robinhood 2024 test file in `testdata/` (secondary test data); Remove Fidelity `_fix_empty_cell_collapse()` (QC Pass 2 handles it)
- **Fidelity "Principal" rows bug (fixed 2026-09-23)**: `_handle_merged_cells()` used a plain substring check (`'INC' in text.upper()`) for company-suffix markers (INC/CORP/LTD/PLC) meant to catch PDF24-merged description rows. Any fully-populated transaction row with `Action = "Principal"` false-matched (contains "**inc**"), wiping the row's Quantity/dates/Proceeds/Cost/Gain-Loss to NaN; the gutted row then got misread by `_is_description_row` as a new parent/description row, corrupting `current_stock_description` for every row after it (e.g. a real Sale row inheriting "Principal" as its description). A second, independent false positive (the `[A-Z]{4,5}` ticker-pattern check matching "WHFIT" inside a footnote paragraph, and "income" inside another) was masking the first — two bugs canceling out is why the existing regression fixtures never caught this. Fix: word-boundary match for the company-suffix keywords, skip rows that already have a valid date (already well-formed transactions), and filter long non-numeric footnote/disclaimer paragraphs out in `_clean_rows` (>100 chars, 0 numeric columns) before they can reach the parent-child logic. Verified via a synthetic 2-Principal + 1-Sale fixture (built with openpyxl — no pandas/openpyxl on this WSL box by default, see below) and the full regression suite. Commit `8b8a1c7`.
- **No pandas/openpyxl/pip on this WSL box by default**: `python3 -m venv <dir> && <dir>/bin/pip install pandas numpy openpyxl` to get a throwaway env for debugging/synthetic repros — network access works fine. `review/.venv` exists but does NOT have pandas (it's for the redactor/review tooling only).

## Regression Suite
- **Location**: `stock_processor/test_regression.py`
- **Run**: `python test_regression.py` — 12/12 green (Fidelity ×2, Merrill, Morgan Stanley, Betterment, Apex Clearing, Robinhood, JP Morgan, Charles Schwab ×4) — verified 2026-09-23, corrects the stale "8/8" count below (Schwab test cases were added in an earlier session)
- **Filter**: `python test_regression.py Merrill` — run only matching tests
- **Verbose**: `python test_regression.py -v` — confirm cell counts on pass
- **Update baselines**: `python test_regression.py --update` (or `Fidelity --update`) — regenerates expected output files
- **Baselines location**: `testdata/passedtestcases/` — input + expected output per broker. **Baselines are manually verified by user against original PDF totals** (one-time human QA). Original PDFs not stored for security. NEVER regenerate baselines (`--update`) without user approval — a failing test means the code broke, not the baseline.
- **Workflow**: run `python test_regression.py` after EVERY broker change before committing
- **Betterment date fix**: Added `MM-DD-YYYY` and `MM-DD-YY` dash patterns to `_format_date()` in `drake_mapper.py`

## Parked / Known Bugs
- **Schwab wash sale bug**: `_standardize_columns` in `schwab.py` blindly maps `Proceeds+3` to "Wash Sale" regardless of actual column count — Gain/Loss values bleed into Wash Sale slot. Fix: detect actual column count before mapping. Schwab is commented out in TEST_CASES in `test_regression.py`.
- **Schwab test files**: input = `testdata/charlesschwab-1099 test.xlsx`, correct row count = 13, Proceeds = $58,507.12, Cost = $37,505.76, Wash Sale = $0 (no wash sales in this file)

## Apex Clearing Notes
- **Layout**: Alternating desc/data rows. Col 0=Date Sold, 1=Qty, 2=Proceeds, 3=(spacer, collapses), 4=Date Acquired, 5=Cost, 6=Wash Sale, 7=Gain/Loss
- **Sheet1**: 9 cols (has "Additional Notes" col 8), Sheet2: 8 cols — broker module handles both
- **Date format**: YYYY-MM-DD (ISO) — added to `_format_date()` in drake_mapper.py
- **Left-shift**: Col 3 spacer collapses, Date Acquired slides left to col 3 → fixed by Pass 2 in pdf_qc.py
- **Parking lot**: `_parse_accrued_wash_sale()` treats all values as Wash Sale; "(M)"/"(D)" markers need handling when non-zero test data available
- **Test**: 11 txns, Proceeds=$3,192.70, Cost=$5,189.38

## JP Morgan Notes
- **Status**: Complete. Manually verified, regression baseline created. 8/8 tests pass.
- **Source**: PDF converted via Excel's built-in import (not PDF24). Single sheet `Append1`, 912 raw rows.
- **Layout**: Cols 0-3 = description/CUSIP, col 4 = qty, col 5 = Date Acquired, col 6 = Date Sold, col 7 = Proceeds, col 8 = Cost, col 9 = Accrued, col 10 = Wash Sale, col 11 = Gain/Loss, col 12 = Additional Info, cols 13-35 = empty padding
- **Options row shift**: On 91/518 rows (PUT/CALL with expiry/strike in cols 2-3), Accrued and Wash Sale shift from cols 9-10 to cols 13-14. Currently handled with fallback in `jpmorgan.py`. **To be replaced by QC Pass 3** (see parking lot).
- **Description pattern**: Transaction rows carry description in cols 0+1. Description-only rows (cols 0-3 text, no dates) appear AFTER the first transaction for a stock and append to the previous transaction's description. Subsequent empty-description rows inherit `current_description`.
- **Repeating headers**: CUSIP / (Box 1a) markers repeat ~34 times through the file — skipped via keyword matching.
- **Regression**: 518 rows × 15 cols = 7,770 cells verified. Baseline at `testdata/passedtestcases/`.
- **Use broker_profiler.py**: Always run on new broker files before building modules — don't write ad-hoc analysis scripts.

## Project: Bank Statement OCR Extractor
- **Location**: `bankdetails_dataextraction/` — standalone Python script (no Streamlit) to extract Citibank checking transactions from scanned image files
- **Script**: `extract_bank_txns.py` — run as `python extract_bank_txns.py "path/to/images/folder" --output result.csv`
- **Dependencies**: `pytesseract==0.3.13`, `Pillow==11.1.0`, Tesseract OCR binary at `C:\Program Files\Tesseract-OCR\tesseract.exe`. Requirements in `requirements.txt`.
- **Input**: Folder of JPG/PNG images (one per page), extracted from PDF via PDF24
- **Output**: CSV with columns: `statement_period, date, description, subtracted, added, balance, flag, source_page`
- **Tested on**: `CCF_000020 images/` (2022, 24 pages, 79 txns) and `CCF_000023-8.jpg` (Zelle-heavy page, 47 txns, exact total match)
- **See detail file**: [bank_ocr.md](bank_ocr.md)

## Project: Tax Return Review Pipeline
- **Location**: `review/` (c904994 2026-09-21 + 2a439bc/11a3f93 2026-09-22; client run folders, `review_runs_names.json`, the venv stay untracked — the user's two spec `.txt` files are now tracked as of 2026-09-22, see below) + `redactor/` (OCR + 9-digit rule ddda057; `confirm_passes` fix 2a439bc). Open items: `review/PARKING_LOT.md`. Status log: `review/SPEC-revision-extraction.md`.
- **What**: compares client source docs (W-2, 1099-INT, 1099-DIV) against the Drake 1040 (check 4). Checks 2a/3 (plausibility of the return alone) are OUT of scope — user decision; `checks.py` was built unasked and removed.
- **drake.py**: 1040 parser, marker-first with label-line fallback, per-year line maps (2024, 2025), arithmetic self-check (13 identities for TY2025). Validated on ONE real TY2025 return: 48/48 lines, 13/13 identities. Unverified: TY2024 map, itemized returns, refund with penalty, returns over 2 pages.
- **Redactor** (`redactor/redact.py`): SSN/EIN plus any standalone 9-digit number (`ID9`, catches undashed EINs; also 9-digit account numbers/CUSIPs) plus entered names and addresses; XMP and non-widget annotations deleted; output filenames scrubbed; name variants auto-matched. Names are typed only in the user's own terminal (`review/redact.py --prompt`, or `redactor/diag_redact.py --prompt` for diagnostics), never stored, never seen by Claude. **When giving CLI examples for either script, lead with `--prompt`, not `--names "..."` as a flag** — a flag example is what led the user to paste a real name into chat on 2026-09-22.
- **Redactor always-on rules (2026-09-24)**: phone (US layout), email, labelled DOB, PAN, Aadhaar, CA employer ID, account numbers (11-18 bare digits or after an account/A-c/Folio label; last 4 kept when 8+), EIN by label, US addresses (street/PO Box/city-state-ZIP across line breaks), India addresses (state/India/place + PIN, whole line), and the line after any address label when it reads like an address. Entered values: DOB/phone/account numbers in every format, addresses punctuation-insensitive. `--prompt` takes a pasted block ending in `END`, shows it back masked. `redactor/probe_labels.py` = masked layout probe (safe to paste). Six items still exposed on `client_bt2024` (CA 540 comb-box account no., designee PIN, PTIN, partial PAN, 8938 "account number or other designation", employer/county/occupation/DOB) — table in `PARKING_LOT.md`. Planned, not built: scan->confirm->redact->purge flow and the return-first harvest alternative (spec, open questions for the user).
- **Redactor 24 Sep (later session)**: two address false positives fixed (numbered field labels under an address label; a lone capital as the city) - the Drake 2024 return went FAIL -> OK. `diag_redact.py` now shows `verify()`'s own view (rule name + masked context), accepts Windows paths, and writes to `redactor/diag_output/` (gitignored). A second outside review still found 9 items (masked table + user decisions in `PARKING_LOT.md`): remove ages/occupation/county; user enters the other preparer's name; no 2-letter name-control matching. Next: fix in the order listed there.
- **Redactor 25 Sep**: 8 of 9 review items fixed as always-on rules (5-digit PIN, PTIN, labelled phone incl. PN/Cell, masked/joined PAN, PAN label, ages, table account no., IRS name controls, address pieces for scan lines). **HARVEST chosen** over scan->confirm (user): nightly batch over `review/redaction_queue/`, double blind, values read from the Drake return in memory only, every file overwritten in place (temp + replace), return last, FAIL -> `review/quarantine/`, REVIEW -> overwrite + flag, no backup. `redactor/harvest.py` (steps 1+2) reads the 1040 header, occupations, phone and refund account by position; verified (masked) on a real full Drake print. Masked report: `harvest.py "<return.pdf>"` -> `diag_output/harvest_*.txt`. Next: step 3 batch driver (PARKING_LOT "DECIDED (25 Sep) for step 3"), then more forms (dependents, state, Sch E, 8938).
- **OCR (`--ocr`, `redactor/redact_ocr.py`, Tesseract)**: image-only pages are OCR'd, matched pixels blacked out, output re-OCR'd; up to 2 extra passes when only OCR re-reads find something (`passes=N`), plus (as of 2026-09-22) `confirm_passes` — one more independent OCR re-read required after the first clean result before it's trusted. **This is still not sufficient by itself**: a real phone-photo W-2 reported `OK` with a real SSN visible TWICE — once from the double-miss case `confirm_passes` now catches, and once AFTER that fix, from OCR consistently failing to read one specific occurrence on every single pass (not random variance). Manual visual check of every OCR'd page is a hard requirement, not a caveat. See `PARKING_LOT.md`.
- **eligibility.py (2026-09-22)**: §5.2 document-level gate — consolidated 1099s, Schedule K-1, 1099-OID, detected from source-document content before redaction/extraction. Confirmed on a real dry run (3 consolidated statements excluded). Return-level items (Sch E/F, Sch C home office, 1099-S, seller-financed mortgage interest) NOT covered.
- **page_filter.py (2026-09-22)**: drops IRS copy-duplicate pages (Copy A/B/C/D/1/2) on W-2/1099-INT/1099-DIV before the model call, distinct from genuine multi-state Box 17 repeats. Known gap: can't yet distinguish a true duplicate from a genuinely different state's copy.
- **Other tools**: `review/img_to_pdf.py` (JPEG/PNG photos -> one-page PDFs, then `redact.py --prompt --ocr`); `review/check4_run.py` (masked Path A driver: own gate, `--dry-run`, index-only output, `--max-tokens` override (default now 4096, was 2048); run by the USER, the API key lives only in their shell). W-2 box 2 compares to line 25a. `redactor/diag_redact.py` (masked diagnostic dump, `--prompt` mode, full OCR-path diagnostics as of 2026-09-22).
- **Data-safety rules**: Claude never reads client names or page text. Printed filename lines count as sensitive. FAIL/REVIEW outputs are never opened and never left in a folder given to `check4_run.py`. Before committing under `review/`: explicit filenames only, then grep the staged diff for names/emails/SSN- and key-shaped strings. The auto-mode classifier blocks ad-hoc page-text dumps; masked probes (booleans, geometry, token shapes) are allowed. `review_runs_redacted/` and `review_runs_names.json` are now in `.gitignore` (added 2026-09-22 — they were untracked and unignored before that, a real near-miss).
- **Run**: `cd review && .venv/bin/python -m unittest test_drake test_redact_wrapper test_img_to_pdf test_eligibility test_page_filter` (72 tests). Redactor: `cd redactor && ../review/.venv/bin/python test_redact.py` (144 checks, script) + `-m unittest test_harvest` (12 tests) + `../review/.venv/bin/python -m unittest test_redact_and_verify` (6 tests).
- **Decisions (user)**: 2026-09-21 — Path A's no-third-party-names rule is lifted for THIS client only (SSN/EIN/typed names still removed); model `claude-sonnet-5`; gate = the user's terminal result plus the driver's own generic re-check. 2026-09-22 — build the eligibility gate at document level only (not return level); §7.2 GROUP 1/GROUP 2 findings confirmed v2 (every v1-scope category has a third-party document, so v1 could only ever produce Group 2 findings).
- **State**: `client_bh1` redacted 11/11 OK (return is file #9) as of 2026-09-21. The 3 phone-photo sources went through `img_to_pdf` + `--ocr`: 1099-INT and one W-2 OK, one W-2 FAILED then redone — and its `OK` status was itself found unreliable (see OCR line above). NEXT: user needs to get a genuinely clean, visually-verified redaction of that W-2 (not solved by code yet), then `check4_run.py --dry-run`, then the real run — still never executed against the live API.
- **Still open**: Path A never run on the API; Path B fails on real multi-page docs (context overflow, `num_ctx` never set); §7.2 report (v2, deferred); return-level §5 eligibility (Sch E/F, Sch C home office, 1099-S, seller-financed mortgage interest); OCR redaction reliability (photo preprocessing or a low-confidence-near-label heuristic, neither built).
